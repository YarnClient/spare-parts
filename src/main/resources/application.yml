spring:
  datasource:
    url: jdbc:mysql://192.168.188.200:3306/spare_parts?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: spare
    password: spare123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: spare-parts-jwt-secret-key-2026-must-be-at-least-256-bits-long!!
    expiration-ms: 604800000  # 7 days

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
